[package]
name = "example-basic"
version = "0.2.1"
edition = "2021"
publish = false

[dependencies]
schema-bridge = { path = "../../crates/schema-bridge" }
serde = { version = "1.0", features = ["derive"] }
